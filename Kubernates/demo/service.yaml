apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: myapp
  type: NodePort
  ports:
  - port: 80             # port of the service
    targetPort: 80        #port of the container/pod
    nodePort: 32000        # expose to local machine