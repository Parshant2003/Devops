name: Env_Variables

on:
    # push
      workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Hello World
      run: echo "Hello World from env.yml"      
    
    # - name: Install
    #   env:
    #     APP_ENV: "Suiiiii"
    #   run: echo "Environment -----> $APP_ENV"   

    - name: Install
      env:
        APP_ENV: "Dev"
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      run: echo "Environment -----> $DB_PASSWORD" 